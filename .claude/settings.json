{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATH\" == *.ts || \"$CLAUDE_FILE_PATH\" == *.tsx ]]; then echo '🔍 Pre-edit: Checking file for lint issues...'; npx eslint \"$CLAUDE_FILE_PATH\" --max-warnings=0 2>/dev/null || echo '⚠️  Lint warnings found (proceeding anyway)'; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATH\" == *.ts || \"$CLAUDE_FILE_PATH\" == *.tsx ]]; then echo '✨ Post-edit: Formatting with Prettier...'; npx prettier --write \"$CLAUDE_FILE_PATH\" 2>/dev/null; echo '🔎 Post-edit: Running TypeScript check...'; npx tsc --noEmit 2>&1 | head -20 || true; fi"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATH\" == *.ts || \"$CLAUDE_FILE_PATH\" == *.tsx ]]; then echo '✨ Post-write: Formatting with Prettier...'; npx prettier --write \"$CLAUDE_FILE_PATH\" 2>/dev/null; fi"
          }
        ]
      }
    ]
  }
}
